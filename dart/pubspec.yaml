name: apiverve_earnings
description: Earnings Report is a tool for retrieving company financial data including income statement, balance sheet, and cash flow data from SEC filings. It supports lookup by ticker or CIK with optional year and quarter filters.
version: 1.1.14
homepage: https://apiverve.com/marketplace/earnings?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/earnings-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
